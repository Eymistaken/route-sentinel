{
  "name": "route-sentinel",
  "version": "1.2.1",
  "private": true,
  "description": "A Chromium extension that stops selected YouTube destinations before playback.",
  "license": "GPL-3.0-only",
  "scripts": {
    "build": "sh scripts/build.sh",
    "test": "node --test tests/*.test.js"
  },
  "engines": {
    "node": ">=20"
  }
}
